// API Gateway Frontend â€” sanitized sample. No bank data.

function describe() {
  return {
    system: "API Gateway Frontend",
    organization: "Jamuna Bank PLC",
    public: true,
    containsSecrets: false,
  };
}

module.exports = { describe };
